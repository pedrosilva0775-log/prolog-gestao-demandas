import { z } from 'zod'; import { identifierSchema,isoDateTimeSchema } from './common'; import { demandListResponseSchema } from './demands';
const queryIds=z.preprocess(value=>typeof value==='string'?value.split(',').map(item=>item.trim()).filter(Boolean):value,z.array(identifierSchema).max(100).optional());
export const reportDemandQuerySchema=z.object({statusIds:queryIds,categoryIds:queryIds,priorityIds:queryIds,userIds:queryIds,teamIds:queryIds,clientIds:queryIds,startDate:isoDateTimeSchema.optional(),endDate:isoDateTimeSchema.optional(),page:z.coerce.number().int().positive().default(1),pageSize:z.coerce.number().int().min(1).max(500).default(500)}).strict().superRefine((value,context)=>{if(value.startDate&&value.endDate&&value.startDate>=value.endDate)context.addIssue({code:'custom',path:['endDate'],message:'A data final deve ser posterior à data inicial.'});});
export const reportDemandListResponseSchema=demandListResponseSchema;
const ids=z.array(identifierSchema); const risk=z.object({id:identifierSchema,sourceDemandId:identifierSchema.optional(),demandName:z.string(),risk:z.string(),impact:z.string(),responsible:z.string(),resolutionDueDate:z.string().optional(),decision:z.string(),severity:z.enum(['low','medium','high','critical']),executiveNote:z.string(),order:z.number()}); const impact=z.object({id:identifierSchema,sourceDemandId:identifierSchema.optional(),title:z.string(),outcome:z.string(),order:z.number()}); const milestone=z.object({id:identifierSchema,sourceDemandId:identifierSchema.optional(),title:z.string(),dueDate:z.string(),progress:z.number().min(0).max(100),risk:z.enum(['low','medium','high']),order:z.number()});
export const reportConfigurationSchema=z.object({statusIds:ids,categoryIds:ids,blocks:z.array(z.enum(['indicators','statusFlow','demandTypes','ongoing','blocked','validation','categoryDistribution','evolution','portfolioHealth','performance','attention','executiveSummary'])),kpis:z.array(z.enum(['total','open','completed','ongoing','blocked','completionRate','validation','cancelled','leadTime','onTime'])),impedimentStatusIds:ids,userIds:ids,clientIds:ids,teamIds:ids,priorityIds:ids,startDate:z.string(),endDate:z.string(),evolutionSeries:z.array(z.enum(['created','completed','ongoing','validation','blocked','cancelled'])),evolutionMonths:z.union([z.literal(3),z.literal(6),z.literal(12)]),autoSummary:z.boolean(),includeRecommendation:z.boolean(),showRisksAndDecisions:z.boolean(),riskItems:z.array(risk),showImpactDeliveries:z.boolean(),impactItems:z.array(impact),showMilestones:z.boolean(),milestoneItems:z.array(milestone)}).strict();
export const reportPresetCreateSchema=z.object({name:z.string().trim().min(2).max(80),configuration:reportConfigurationSchema}).strict();
export const reportPresetDtoSchema=z.object({id:identifierSchema,name:z.string(),configuration:reportConfigurationSchema,updatedAt:isoDateTimeSchema}).strict(); export const reportPresetListSchema=z.array(reportPresetDtoSchema);
