import { describe, expect, it } from 'vitest'; import { hashPassword, validPassword, verifyPasswordHash } from './password.js';
describe('credenciais',()=>{it('gera hash scrypt com salt e valida a senha',()=>{const hash=hashPassword('PrologSeguro123');expect(hash).not.toContain('PrologSeguro123');expect(verifyPasswordHash('PrologSeguro123',hash)).toBe(true);expect(verifyPasswordHash('errada',hash)).toBe(false);});it('exige letras, números e doze caracteres',()=>{expect(validPassword('PrologSeguro123')).toBe(true);expect(validPassword('Prolog123')).toBe(false);expect(validPassword('somenteletras')).toBe(false);expect(validPassword('123456789012')).toBe(false);});});
