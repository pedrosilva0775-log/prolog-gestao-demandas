import { describe, expect, it } from 'vitest'; import { hashPassword, validPassword, verifyPasswordHash } from './password.js';
describe('credenciais',()=>{it('gera hash scrypt com salt e valida a senha',()=>{const hash=hashPassword('Prolog123');expect(hash).not.toContain('Prolog123');expect(verifyPasswordHash('Prolog123',hash)).toBe(true);expect(verifyPasswordHash('errada',hash)).toBe(false);});it('exige letras, números e oito caracteres',()=>{expect(validPassword('Prolog123')).toBe(true);expect(validPassword('somenteletras')).toBe(false);expect(validPassword('12345678')).toBe(false);});});
